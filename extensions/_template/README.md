# Reusable Extension Template

Copy this directory to `extensions/<package-code>/` when building a reusable
plugin, connector, report package, skill package, or integration asset.

Do not use this directory for one-customer business modules. Those belong in
`business/<project-code>/`.

Do not use this directory for customer deployment overlays. Those belong in
`customer/<project-code>/`.
