**改动摘要**

只修改了 [backend/tests/integration/ai/test_context_engine_capabilities.py](E:/git_clone/novusai-saas-yudi/backend/tests/integration/ai/test_context_engine_capabilities.py)。

这次修复包含两部分：
- 给 `_BaseEngineStub` 补齐 `_build_web_research_continuation_context()`，返回空的 `ResearchContinuationContext`，对齐当前 `ConversationContextEngine` 的调用契约。
- 把这组测试从真实 `IntentPlanner` 启发式解耦，新增显式 `IntentPlan` 测试夹具并在 `_assemble_context()` 中 patch `IntentPlanner.plan_turn`，让断言稳定覆盖“能力感知诊断/locale hint”而不是依赖自然语言意图推断。

**测试命令**

```bash
python -m pytest backend/tests/integration/ai/test_context_engine_capabilities.py -q
```

**测试结果**

```text
7 passed, 1 warning in 2.92s
```

补充：唯一 warning 是 `backend/.pytest_cache` 写入权限告警，不影响本次测试通过。

**变更文件绝对路径**

- [backend/tests/integration/ai/test_context_engine_capabilities.py](E:/git_clone/novusai-saas-yudi/backend/tests/integration/ai/test_context_engine_capabilities.py)