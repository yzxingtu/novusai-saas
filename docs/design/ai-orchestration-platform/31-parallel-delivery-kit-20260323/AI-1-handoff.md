# AI-1 Handoff

## 0. 冻结信息

- `AI 编号`：
- `负责人`：
- `工作副本路径`：
- `分支名`：
- `最后提交 SHA`：
- `冻结时间`：
- `对应 prompt 版本`：`31` / `32` / `33`

## 1. 负责范围回顾

- `负责命名空间`：
- `明确未触碰的目录/文件`：
- `本次实现目标`：

## 2. 新增或修改文件清单

按下面格式逐项填写，不要只写目录：

| 文件路径 | 类型 | 用途 | 是否最终版 |
|---|---|---|---|
| `backend/app/...` | `new/update` | 待填写 | `yes/no` |

## 3. 需要串行注册到共享文件的项

### 3.1 `backend/app/models/__init__.py`

- 需导出的模型：
- 导出顺序建议：
- 是否依赖 `AI-2` 模型引用：

### 3.2 `backend/app/api/admin/__init__.py`

- 需注册的 router：
- 建议 include 顺序：
- 父资源或菜单归属：

### 3.3 `backend/app/locales/en/messages.json`

- 新增 message keys：
- 对应用途：

### 3.4 `backend/app/locales/zh_CN/messages.json`

- 新增 message keys：
- 对应用途：

### 3.5 `backend/app/locales/en/menu.json`

- 新增 menu keys：
- 对应页面：

### 3.6 `backend/app/locales/zh_CN/menu.json`

- 新增 menu keys：
- 对应页面：

## 4. 统一迁移登记

- `新增表`：
- `新增枚举`：
- `新增索引/唯一约束`：
- `新增外键`：
- `需要显式命名的约束`：
- `与 AI-2 运行时域的关联表/外键`：
- `迁移生成顺序建议`：

## 5. 设计时对象契约输出

本节是提供给 `AI-2`、`AI-3`、`AI-4` 和 `integrator` 的共享真相，请至少覆盖：

### 5.1 `solution`

- 主字段：
- 状态枚举：
- 关键外键：
- 可过滤/可排序字段：

### 5.2 `workflow`

- 主字段：
- 状态枚举：
- 关键外键：
- 可过滤/可排序字段：

### 5.3 `release`

- 主字段：
- 状态枚举：
- 关键外键：
- 可过滤/可排序字段：

### 5.4 `trigger`

- 主字段：
- 状态枚举：
- 关键外键：
- 可过滤/可排序字段：

### 5.5 `environment`

- 主字段：
- 状态枚举：
- 关键外键：
- 可过滤/可排序字段：

### 5.6 `change_set`

- 主字段：
- 状态枚举：
- 关键外键：
- 可过滤/可排序字段：

## 6. 权限、菜单与接口摘要

- `permission_resource` 清单：
- 计划使用的 `parent_resource`：
- 管理端 API 一览：
- 需要前端接入的页面 key / menu key：
- 依赖 `AI-2` 的字段或状态：

## 7. 已执行验证

- `pytest` 文件与结果：
- 手工 API 冒烟：
- 未执行但建议集成人补跑的校验：

## 8. 已知问题、阻塞与集成建议

- `已知问题`：
- `临时假设`：
- `阻塞项`：
- `建议集成顺序`：
- `如果契约冲突，建议以谁为准`：
