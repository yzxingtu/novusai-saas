# AI-3 Handoff

## 0. 冻结信息

- `AI 编号`：
- `负责人`：
- `工作副本路径`：
- `分支名`：
- `最后提交 SHA`：
- `冻结时间`：
- `对应 prompt 版本`：`31` / `32` / `33`

## 1. 负责范围回顾

- `负责命名空间`：
- `明确未触碰的目录/文件`：
- `本次实现目标`：

## 2. 新增或修改文件清单

按下面格式逐项填写，不要只写目录：

| 文件路径 | 类型 | 用途 | 是否最终版 |
|---|---|---|---|
| `frontend/apps/web-antd/src/...` | `new/update` | 待填写 | `yes/no` |

## 3. 页面与 API 对应关系

| 页面/模块 | 主要文件 | 依赖 API 文件 | 依赖后端对象 |
|---|---|---|---|
| 待填写 | 待填写 | 待填写 | 待填写 |

## 4. 需要集成人补的共享接入项

### 4.1 页面入口与导航接入

- 需要修改的共享文件：
- 目标路由路径：
- route meta / menu meta 建议：
- 页面排序建议：

### 4.2 后端菜单、权限与资源 key

- 依赖的 `permission_resource`：
- 依赖的 action：
- 依赖的 menu key：
- 依赖的 parent menu / parent resource：

### 4.3 对后端契约的依赖

- 依赖的接口：
- 依赖的字段：
- 依赖的状态枚举：
- 当前是否存在 mock / 占位字段假设：

### 4.4 Locale 聚合与文案接入

- 新增 i18n 文件：
- 文案前缀：
- 是否需要调整共享 locale 入口：

## 5. 关键截图与联调证据

- 关键页面截图路径：
- 截图对应页面：
- 已完成的浏览器冒烟：
- 仍未联调的页面：

## 6. 已执行校验

- `pnpm typecheck`：
- 其他前端校验：
- 未执行但建议集成人补跑的校验：

## 7. 已知问题、阻塞与集成建议

- `已知问题`：
- `临时假设`：
- `阻塞项`：
- `建议与 AI-1 / AI-2 对齐的字段`：
- `建议集成人最后处理的共享文件`：
