# Integrator 最终集成报告模板（2026-03-23）

> 使用说明：
> 1. 本文档给 `integrator` 在完成串行收口后直接复制填写。
> 2. 报告不要求写成长文，但必须把共享文件接入、迁移、验证、冲突裁决和遗留问题写清。
> 3. 这份报告是最终交付物之一，不应省略。

---

## 一、集成批次信息

- `批次名称`：
- `集成人`：
- `集成工作副本路径`：
- `集成分支名`：
- `集成开始时间`：
- `集成完成时间`：
- `基准 SHA`：
- `最终提交 SHA`：

---

## 二、输入物确认

| 输入项 | 是否收到 | 备注 |
|---|---|---|
| `AI-1` 分支名与 SHA | `yes/no` |  |
| `AI-2` 分支名与 SHA | `yes/no` |  |
| `AI-3` 分支名与 SHA | `yes/no` |  |
| `AI-4` 分支名与 SHA | `yes/no` |  |
| 4 份 handoff | `yes/no` |  |
| 主协调者冻结签收单 | `yes/no` |  |
| 主协调者正式移交单 | `yes/no` |  |
| 契约矩阵 `33` | `yes/no` |  |

---

## 三、实际合并顺序

1. 
2. 
3. 
4. 
5. 
6. 

如与建议顺序不一致，请说明原因：

- 待填写

---

## 四、合并来源摘要

### 4.1 `AI-1`

- `分支名`：
- `提交 SHA`：
- `主要并入内容`：
- `是否存在额外修补`：`yes/no`
- `额外修补说明`：

### 4.2 `AI-2`

- `分支名`：
- `提交 SHA`：
- `主要并入内容`：
- `是否存在额外修补`：`yes/no`
- `额外修补说明`：

### 4.3 `AI-3`

- `分支名`：
- `提交 SHA`：
- `主要并入内容`：
- `是否存在额外修补`：`yes/no`
- `额外修补说明`：

### 4.4 `AI-4`

- `分支名`：
- `提交 SHA`：
- `主要并入内容`：
- `是否存在额外修补`：`yes/no`
- `额外修补说明`：

---

## 五、共享文件接入清单

### 5.1 后端共享文件

| 文件 | 是否修改 | 接入项摘要 |
|---|---|---|
| `backend/app/models/__init__.py` | `yes/no` |  |
| `backend/app/api/admin/__init__.py` | `yes/no` |  |
| `backend/app/api/tenant/__init__.py` | `yes/no` |  |
| `backend/app/locales/en/messages.json` | `yes/no` |  |
| `backend/app/locales/zh_CN/messages.json` | `yes/no` |  |
| `backend/app/locales/en/menu.json` | `yes/no` |  |
| `backend/app/locales/zh_CN/menu.json` | `yes/no` |  |
| `backend/migrations/env.py` | `yes/no` |  |
| `backend/app/core/metrics.py` | `yes/no` |  |

### 5.2 前端共享文件

| 文件 | 是否修改 | 接入项摘要 |
|---|---|---|
| `frontend/apps/web-antd/src/api/admin/index.ts` | `yes/no` |  |
| `frontend/apps/web-antd/src/api/tenant/index.ts` | `yes/no` |  |
| `frontend/apps/web-antd/src/locales/index.ts` | `yes/no` |  |
| 其他共享入口/路由文件 | `yes/no` |  |

---

## 六、迁移与指标收口

### 6.1 Alembic 迁移

- `是否生成统一迁移`：`yes/no`
- `迁移文件名`：
- `涉及表`：
- `涉及外键/索引/约束`：
- `是否完成迁移验证`：`yes/no`

### 6.2 Metrics

- `是否补充 metrics.py`：`yes/no`
- `新增指标清单`：
- `未补齐但已登记的指标缺口`：

---

## 七、契约冲突与裁决记录

### 7.1 字段或状态冲突

| 冲突对象 | 冲突描述 | 裁决依据 | 最终处理 |
|---|---|---|---|
| 待填写 | 待填写 | `33` / handoff / 主协调者说明 | 待填写 |

### 7.2 偏离 handoff 的修补

| 文件/对象 | 偏离原因 | 修补内容 | 是否已在最终状态反映 |
|---|---|---|---|
| 待填写 | 待填写 | 待填写 | `yes/no` |

---

## 八、验证矩阵结果

### 8.1 后端

- `pytest`：
- `alembic upgrade`：
- `关键 API 冒烟`：

### 8.2 前端

- `pnpm typecheck`：
- `管理端关键页面冒烟`：
- `企业端关键页面冒烟`：

### 8.3 联调

- `admin / tenant 严格分端`：
- `前后端字段一致性`：
- `推荐中心结构化展示`：
- `审批 / 运行 / 激活最小闭环`：

---

## 九、遗留问题与风险

### 9.1 未解决问题

- 待填写

### 9.2 已知风险

- 待填写

### 9.3 建议下一阶段优先处理

1. 
2. 
3. 

---

## 十、最终交付物确认

| 交付物 | 是否完成 | 备注 |
|---|---|---|
| 最终集成分支 | `yes/no` |  |
| 统一迁移文件 | `yes/no` |  |
| 已更新共享文件 | `yes/no` |  |
| 最小验证结果 | `yes/no` |  |
| 本最终集成报告 | `yes/no` |  |

---

## 十一、签字区

- `集成人`：
- `完成时间`：
- `主协调者复核`：
- `复核时间`：
