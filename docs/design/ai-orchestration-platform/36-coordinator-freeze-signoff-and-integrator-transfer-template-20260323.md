# 主协调者冻结签收单与集成人正式移交单模板（2026-03-23）

> 使用说明：
> 1. 本文档给 `coordinator` 直接复制填写。
> 2. 先填写“冻结签收单”，确认 4 个 AI 都达到 `frozen_accepted`。
> 3. 再填写“正式移交单”，将并行交付正式移交给 `integrator`。
> 4. 不要口头移交，必须保留书面版本。

---

## 一、并行交付批次信息

- `批次名称`：
- `项目/主题`：AI 编排平台第一阶段实施
- `主协调者`：
- `集成人`：
- `基准仓库路径`：
- `基准提交 SHA`：
- `并行启动时间`：
- `冻结截止时间`：
- `移交时间`：

---

## 二、角色映射表

| 角色 | 执行者 | 工作副本路径 | 分支名 | handoff 文件 |
|---|---|---|---|---|
| `AI-1` |  |  |  |  |
| `AI-2` |  |  |  |  |
| `AI-3` |  |  |  |  |
| `AI-4` |  |  |  |  |
| `integrator` |  |  |  | `N/A` |

---

## 三、冻结签收单

### 3.1 `AI-1` 冻结签收

- `最后提交 SHA`：
- `handoff 是否完整`：`yes/no`
- `是否误改冻结文件`：`yes/no`
- `是否自行生成迁移`：`yes/no`
- `已提供测试/校验证据`：
- `主要风险/阻塞`：
- `签收状态`：`prepared/in_progress/handoff_incomplete/frozen_rejected/frozen_accepted/blocked`
- `主协调者备注`：

### 3.2 `AI-2` 冻结签收

- `最后提交 SHA`：
- `handoff 是否完整`：`yes/no`
- `是否误改冻结文件`：`yes/no`
- `是否自行生成迁移`：`yes/no`
- `已提供测试/校验证据`：
- `主要风险/阻塞`：
- `签收状态`：`prepared/in_progress/handoff_incomplete/frozen_rejected/frozen_accepted/blocked`
- `主协调者备注`：

### 3.3 `AI-3` 冻结签收

- `最后提交 SHA`：
- `handoff 是否完整`：`yes/no`
- `是否误改冻结文件`：`yes/no`
- `是否自行生成迁移`：`N/A`
- `已提供测试/校验证据`：
- `主要风险/阻塞`：
- `签收状态`：`prepared/in_progress/handoff_incomplete/frozen_rejected/frozen_accepted/blocked`
- `主协调者备注`：

### 3.4 `AI-4` 冻结签收

- `最后提交 SHA`：
- `handoff 是否完整`：`yes/no`
- `是否误改冻结文件`：`yes/no`
- `是否自行生成迁移`：`N/A`
- `已提供测试/校验证据`：
- `主要风险/阻塞`：
- `签收状态`：`prepared/in_progress/handoff_incomplete/frozen_rejected/frozen_accepted/blocked`
- `主协调者备注`：

---

## 四、冻结通过汇总

- `是否 4 个 AI 全部达到 frozen_accepted`：`yes/no`
- `未通过角色`：
- `退回补齐事项`：
- `是否允许进入正式移交`：`yes/no`
- `主协调者签字/标识`：

---

## 五、已知冲突与重点核查事项

### 5.1 字段或状态冲突

- 待填写

### 5.2 共享文件接入重点

- 待填写

### 5.3 未完成真实联调的页面或接口

- 待填写

### 5.4 必须由 `integrator` 统一完成的事项

- 统一迁移
- 共享 locale 合并
- 共享菜单与权限接入
- 共享 API 入口接入
- 共享 metrics 补齐
- 其他：

---

## 六、正式移交单

### 6.1 移交输入物清单

| 项目 | 是否已提供 | 备注 |
|---|---|---|
| `AI-1` 分支名与 SHA | `yes/no` |  |
| `AI-2` 分支名与 SHA | `yes/no` |  |
| `AI-3` 分支名与 SHA | `yes/no` |  |
| `AI-4` 分支名与 SHA | `yes/no` |  |
| 4 份 handoff | `yes/no` |  |
| 冻结签收结果 | `yes/no` |  |
| 已知冲突清单 | `yes/no` |  |
| 建议合并顺序 | `yes/no` |  |
| 不允许自行猜测的风险点 | `yes/no` |  |

### 6.2 建议合并顺序

1. `AI-1`
2. `AI-2`
3. 后端共享文件收口
4. `AI-3`
5. `AI-4`
6. 前端共享文件收口
7. 验证矩阵

### 6.3 不允许 `integrator` 自行猜测的风险点

- 待填写

### 6.4 移交说明

- `handoff 质量最好，可优先参考的角色`：
- `handoff 中仍带临时假设的角色`：
- `需要重点核查的对象`：
- `需要重点核查的页面/接口`：

### 6.5 正式移交确认

- `主协调者`：
- `移交时间`：
- `集成人接收确认`：
- `接收时间`：

---

## 七、附录：建议附件

- 角色与工作副本映射表
- 分支与冻结提交表
- 4 份 handoff 原件
- 关键页面截图
- 关键测试/校验结果摘要
