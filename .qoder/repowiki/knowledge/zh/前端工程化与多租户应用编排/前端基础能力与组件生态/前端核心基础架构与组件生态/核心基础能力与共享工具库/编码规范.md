- 所有子包均遵循 ESM 规范，并在 `package.json` 中明确声明 `"type": "module"`。
- 工具函数与常量采用按功能域划分的目录结构（如 `utils`, `color`, `cache`），并通过 `index.ts` 统一聚合导出。
- 类型定义包（typings）通过 `.d.ts` 文件扩展全局命名空间（如 `vue-router` 增强），并提供统一的类型入口。