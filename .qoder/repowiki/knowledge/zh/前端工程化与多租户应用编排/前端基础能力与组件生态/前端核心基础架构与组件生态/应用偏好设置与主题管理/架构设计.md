- 核心采用单例 `PreferenceManager` 类（`preferences.ts`），基于 Vue 3 `reactive` 实现状态响应式。
- 存储层集成 `StorageManager` 实现配置的本地持久化与防抖保存。
- 表现层通过 `usePreferences` Composable 暴露计算属性，供业务组件订阅布局或功能开关状态。
- 样式同步模块 `update-css-variables.ts` 负责将主题配置实时映射为 DOM CSS 变量及暗黑模式类名。