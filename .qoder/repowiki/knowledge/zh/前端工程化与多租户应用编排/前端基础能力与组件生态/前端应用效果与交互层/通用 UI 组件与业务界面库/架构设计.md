- 采用双层导出架构：`src/components` 封装原子化交互组件，`src/ui` 聚合复杂业务场景视图。
- 依赖 `@vben-core/shadcn-ui` 等核心库实现底层 UI，并通过 `src/index.ts` 统一对外暴露 API。
- 支持模块化导入，在 `package.json` 中通过 `exports` 字段对 `tippy`、`loading` 等高频组件提供独立入口以优化打包体积。