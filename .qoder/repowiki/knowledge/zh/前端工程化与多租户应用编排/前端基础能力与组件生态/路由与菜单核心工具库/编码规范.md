- 所有辅助函数均采用命名导出（Named Exports），并通过 `index.ts` 进行二次聚合导出以简化调用路径。
- 路由与菜单处理逻辑普遍采用递归树形遍历模式，配合 `@vben-core/shared/utils` 提供的 `mapTree` 和 `filterTree` 实现深层节点过滤与转换。
- 函数实现遵循纯函数优先原则，除 `unmount-global-loading` 等直接操作 DOM 的工具外，核心逻辑如 `generateRoutesByFrontend` 均不产生副作用。