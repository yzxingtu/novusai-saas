- 采用扁平化模块结构，`src/` 下按功能域（如 `fs.ts`, `git.ts`, `monorepo.ts`）划分独立文件。
- `src/index.ts` 作为统一入口，聚合导出所有工具函数及第三方依赖（如 `chalk`, `execa`, `consola`），形成单一 API 表面。
- 构建配置 `build.config.ts` 使用 `unbuild` 进行打包，生成 ESM 格式产物及类型声明。
- 依赖方向明确：内部模块仅依赖 Node.js 内置模块及指定的第三方库，无循环依赖。