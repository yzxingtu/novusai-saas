- 采用功能子目录划分：authentication（登录/注册/找回密码）、fallback（403/404/500等异常页）、profile（个人设置）及about。
- 视图层高度依赖 `@vben/common-ui` 提供的封装组件（如 `AuthenticationLogin`、`Fallback`、`Profile`），本地代码主要负责业务逻辑组装与表单配置。
- 通过 `#store` 路径别名对接 Pinia 状态管理（如 `useAuthStore`），实现认证状态的持久化与交互。