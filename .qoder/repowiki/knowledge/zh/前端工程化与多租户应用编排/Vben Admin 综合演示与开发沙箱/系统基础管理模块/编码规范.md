- 列表页与数据配置分离：将表格列定义和表单字段配置抽取到 data.ts 文件中，通过 `useColumns` 和 `useSchema` 函数返回配置，支持动态多语言切换。
- 统一的 CRUD 交互模式：列表页负责触发新增、编辑、删除操作，表单组件负责数据校验与提交，成功后通过 emit('success') 通知列表页刷新。
- 树形结构处理：部门和菜单模块在表格配置中启用 `treeConfig`，支持父子节点关联与层级展示，新增时支持“添加下级”功能。