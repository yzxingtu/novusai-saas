- CLI 工具均采用 TypeScript 编写源码，并通过 `bin/*.mjs` 作为 ESM 入口点加载编译后的产物或 stub。
- 所有异步文件操作优先使用 `node:fs/promises` 配合 `async/await`，并在遍历目录时实施并发限制（如 `CONCURRENCY_LIMIT`）。
- 脚本中的错误处理统一捕获并输出到 `stderr`，关键操作失败时通过 `process.exit(1)` 终止进程以确保 CI/CD 流水线中断。
- 多语言支持体现在代码注释中，关键逻辑处同时保留中文与英文说明，便于国际化团队协作。