- 所有子项目统一继承 `internal` 提供的 ESLint、Prettier 及 Stylelint 配置，确保代码风格与质量门禁一致。
- 采用统一的 TypeScript 配置基类（`base.json`, `web-app.json`），规范各模块的类型检查严格度与路径别名。
- 通过 `catalog:` 协议在 `pnpm-workspace.yaml` 中集中管理跨模块共享依赖的版本，避免依赖冲突。