- 所有模块目录均包含 `index.ts` 作为统一导出入口，遵循模块化封装原则。
- 复杂业务逻辑（如 AI 聊天、文件上传）优先拆分为独立的 composables（以 `use-` 开头），并在组件中按需组合。
- API 请求统一通过 `#/utils/request` 导出的 `requestClient` 发起，并依赖拦截器处理认证与错误。
- 类型定义集中在 `#/types` 或通过 `#/api/shared/types` 共享，确保前后端接口契约的一致性。