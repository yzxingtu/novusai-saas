- **路由与布局隔离**：通过 `adminRoutes` 定义独立于租户端的 `/admin` 命名空间，采用 `AuthPageLayout` 与 `BasicLayout` 实现认证态与非认证态的物理隔离。
- **动态菜单驱动**：除登录、仪表盘及代码生成器等核心静态路由外，大部分业务模块（如系统配置、租户列表）依赖后端动态下发的菜单数据进行渲染，实现权限与视图的解耦。
- **API 模块化聚合**：`api/admin/index.ts` 作为统一出口，将 AI、租户、系统日志等分散的业务接口按领域收敛，供各子模块按需引用，维持了跨域调用的契约一致性。