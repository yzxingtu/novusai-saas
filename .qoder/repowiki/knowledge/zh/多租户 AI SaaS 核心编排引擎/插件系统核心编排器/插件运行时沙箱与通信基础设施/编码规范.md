- 所有对外暴露的能力接口（如数据库、存储、HTTP）在执行前必须调用 `_require(capability)` 进行显式授权校验。
- 插件相关的数据库操作必须通过 `PluginDbProxy` 代理执行，禁止直接暴露或访问原始 `AsyncSession`。
- 跨插件通信的事件命名严格遵循 `plugin.{source_plugin}.{event_name}` 格式，以确保全局唯一性。
- 涉及文件系统路径解析时，统一使用 `PurePosixPath` 进行规范化处理，并校验解析后的绝对路径是否位于允许的根目录内。