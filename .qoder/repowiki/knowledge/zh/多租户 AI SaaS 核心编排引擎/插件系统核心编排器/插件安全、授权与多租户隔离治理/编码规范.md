- 敏感配置字段通过 JSON Schema 的 `x-encrypted` 扩展属性声明，由 `crypto.py` 统一处理持久化加密与运行时解密。
- 插件安全扫描采用白名单机制，对 `importlib` 等模块的子模块（如 `importlib.util`）进行精细化放行，避免误报。
- License 验证逻辑在 DEBUG 模式下支持跳过签名校验的降级处理，但在生产环境中强制要求配置公钥。
- 前置校验（Preflight）与生命周期守卫（Guards）采用注册表模式（Registry Pattern），支持按优先级动态注入业务规则。