- 所有改变插件状态的核心方法（enable, disable, uninstall）均通过 `_plugin_lock` 上下文管理器包裹，以防止并发修改。
- 采用 Mixin 模式拆分职责，将安装、迁移、依赖管理和运行时状态逻辑分别封装在独立的 Mixin 类中，最后由主类聚合。
- 在执行破坏性操作（如禁用、卸载）前，统一调用 `run_plugin_lifecycle_guards` 进行前置校验，支持宿主级阻断策略。