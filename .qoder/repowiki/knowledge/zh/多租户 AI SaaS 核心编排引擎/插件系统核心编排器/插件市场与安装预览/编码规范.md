- 所有外部资源 URL（特别是插件下载源）必须经过 `validate_github_source_url` 白名单校验，确保仅允许 `github.com` 及其相关子域名。
- 插件 ZIP 包处理时必须执行严格的安全扫描，包括禁止符号链接、校验解压路径不超出根目录、限制压缩比以防炸弹攻击。
- 市场数据获取采用“远程优先、缓存次之、本地回退”的三级容错策略，确保在网络异常时仍能提供基础服务。
- 预览功能中的多语言字段（如名称、描述）通过 `resolve_i18n` 工具函数根据当前 Locale 动态解析，并提供完善的降级逻辑。