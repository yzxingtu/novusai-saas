- 所有插件扩展的注册键（Key）均采用 `plugin.{plugin_name}.{identifier}` 或 `{plugin_name}:{skill_name}` 等带命名空间的格式，以防止全局冲突。
- 扩展注册时均调用 `_track` 方法记录 `RegisteredExtension` 对象，确保后续能通过 `unregister_all` 完整清理插件占用的系统资源。
- 在处理插件声明的类（如 Executor 或 Middleware）时，遵循“类则实例化缓存，实例则直接使用”的模式，避免每次调用重复创建开销。