- 所有插件主类必须继承 `PluginBase` 并位于约定路径 `backend/plugins/{name}/backend/main.py`。
- 动态加载模块时统一采用 `plugins.{plugin_name}.backend.{dotted_path}` 命名规范，并在失败时主动清理 `sys.modules` 中的破损缓存。
- 插件元数据文件 `plugin.yaml` 必须包含在插件根目录下，且大小限制在 1MB 以内以确保加载性能。