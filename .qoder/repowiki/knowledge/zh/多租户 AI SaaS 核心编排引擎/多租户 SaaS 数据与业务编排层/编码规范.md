- 所有模型继承自 `BaseModel` 或 `TenantModel`，自动获得软删除、时间戳及表名蛇形命名转换能力。
- 采用声明式删除保护机制，通过模型中的 `__delete_deps__` 属性定义外键依赖的处理策略（如 BLOCK, CASCADE_SOFT）。
- 业务逻辑封装在 Service 层，并通过 Facade 模式（如 `AdminAuthFacade`）将复杂认证流程拆解为独立领域对象。
- Schema 定义按 `common`、`system`、`tenant`、`ai` 物理隔离，但共享基础校验逻辑与分页参数结构。