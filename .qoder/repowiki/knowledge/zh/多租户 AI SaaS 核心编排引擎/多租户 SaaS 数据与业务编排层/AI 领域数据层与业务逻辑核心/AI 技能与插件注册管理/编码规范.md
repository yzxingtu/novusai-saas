- 在 Repository 查询中强制注入多租户可见性逻辑，即同时匹配 `tenant_id = current_tenant` 或 `tenant_id IS NULL`（平台级共享），并过滤已退役（retired）的资源。
- 对系统内置技能（`is_system=True`）实施写保护，禁止在 Service 层修改其类型、激活状态或删除操作，仅允许更新非关键配置。
- 在 Skill 创建或更新前，若类型为 Toolkit 且开启了安全扫描，则调用 `_scan_toolkit_security` 检查代码内容，防止注入恶意脚本。
- 支持 AgentScope 规范的 `skill_md` 字段，在 Schema 层通过 `model_validator` 自动解析 Markdown 元数据并同步至 `key` 和 `description` 字段。