- **异步 ORM**：基于 SQLAlchemy 2.0+ 的 `async_session` 和 `Mapped` 类型注解进行异步数据库操作。
- **数据校验**：使用 Pydantic V2 的 `model_validator` 和 `Field` 进行严格的输入 schema 校验（如 `AgentChatRequest`）。
- **流式响应**：通过 FastAPI 的 `StreamingResponse` 结合自定义的 `ExecutionDispatcher` 和 `ConversationEngine` 实现服务端推送事件（SSE）风格的流式对话。