- **分层架构**：严格遵循 Model（ORM 映射）-> Repository（数据访问抽象）-> Schema（接口契约）-> Service（业务逻辑）的四层结构，实现数据持久化与业务逻辑的解耦。
- **领域驱动设计**：围绕 Agent、Knowledge Base、Skill 等核心聚合根组织代码，Service 层通过组合多个 Repository 完成复杂的跨实体事务与状态流转。
- **读写分离倾向**：在监控与诊断场景中，存在专门的 Read Model Service 与 Projector，将运行时产生的复杂对话轨迹投影为易于查询的监控视图，与核心写入路径隔离。