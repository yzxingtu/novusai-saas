- 所有仓储类继承自 `TenantRepository` 并定义 `_scope_fields` 以声明前端可筛选/排序的字段白名单。
- 服务类继承自 `TenantService`，在 `_before_create` 钩子中自动注入 `tenant_id`，无需在业务代码中手动赋值。
- 模型类通过 `__delete_deps__` 声明删除依赖策略（BLOCK/CASCADE_SOFT），由基类统一执行级联删除或阻断检查。
- Schema 中的敏感字段（如头像、密钥）通过 `field_validator` 进行标准化处理或脱敏，区分写入 ID 与响应展示逻辑。