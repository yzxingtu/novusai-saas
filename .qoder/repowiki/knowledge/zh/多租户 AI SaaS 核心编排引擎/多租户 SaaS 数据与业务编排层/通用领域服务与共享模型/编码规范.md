- 服务类统一接收 `AsyncSession` 作为构造参数以管理数据库事务上下文。
- 通知渠道实现遵循 `NotificationChannel` 抽象基类，必须实现 `channel_code` 属性与 `deliver` 方法。
- 认证逻辑按角色拆分为独立 Domain 类，并通过 Facade 模式在 `AuthService` 中统一暴露接口。
- Schema 定义继承自 `BaseSchema`，并利用 Pydantic 的 `Field` 和 `validator` 进行输入约束。