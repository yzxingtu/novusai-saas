- 所有业务逻辑层（Service/Repository）必须通过依赖注入获取 `AsyncSession`，并遵循“异常即回滚”的事务管理原则。
- 跨模块通信优先采用声明式扩展点（如插件 `extensions` 字段或 `SystemHookPoint`），严禁直接硬编码引用其他子模块的内部实现类。
- API 响应统一使用 `app.core.response` 提供的封装函数（如 `success`, `error`），确保返回结构包含 `code`, `message`, `data` 标准字段。