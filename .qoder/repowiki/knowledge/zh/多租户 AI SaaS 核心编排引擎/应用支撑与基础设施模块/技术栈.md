- **ASGI 中间件**：基于 Starlette 纯 ASGI 接口实现 `TenantMiddleware` 和 `TraceIdMiddleware`，避免 BaseHTTPMiddleware 的取消级联问题。
- **实时通信**：使用 `python-socketio` 构建多租户隔离的 WebSocket 命名空间，集成 Redis 进行在线状态管理 (`presence.py`)。
- **图像处理**：`utils/image.py` 封装 Pillow 库，提供异步线程池驱动的图片缩放、裁剪及格式转换能力。