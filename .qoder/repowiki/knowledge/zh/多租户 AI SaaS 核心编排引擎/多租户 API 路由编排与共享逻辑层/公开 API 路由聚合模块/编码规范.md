- 所有公开端点必须显式添加 `@public` 装饰器以绕过 RBAC 权限检查。
- 业务成功响应统一调用 `success(data=..., message=...)` 返回标准化字典结构。
- 多租户相关接口通过 `get_tenant_context(request)` 获取当前租户上下文，并在未解析时抛出 404。
- 涉及敏感操作或频率限制的接口（如验证码、图片处理）需集成基于 IP 或上下文的速率限制逻辑。