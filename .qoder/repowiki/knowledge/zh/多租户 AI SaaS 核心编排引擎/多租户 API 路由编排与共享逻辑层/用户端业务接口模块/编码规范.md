- 所有非公开接口均使用 `@auth_only` 装饰器进行身份验证保护，公开接口（如登录、注册）使用 `@public` 标记。
- 在涉及多文件组织的业务模块中，优先采用继承 `BaseController` 的类来封装路由逻辑，以实现代码的结构化分组。
- 所有异步数据库操作后，在返回响应前显式调用 `await db.commit()` 以确保事务持久化。
- 接口返回值统一使用 `success`、`paginated` 或 `deleted` 等标准化响应包装函数，保持 API 响应格式一致。