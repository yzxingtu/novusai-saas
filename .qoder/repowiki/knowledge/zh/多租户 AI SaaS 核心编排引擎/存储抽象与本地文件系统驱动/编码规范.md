- 所有涉及文件系统读写、移动或删除的操作均封装在同步函数中，并通过 `await anyio.to_thread.run_sync()` 调用，以防止阻塞事件循环。
- 本地驱动采用侧写文件（Sidecar File）模式，即在与原文件相同路径下创建 `.meta.json` 文件来存储 MIME 类型、可见性等元数据。
- 在执行文件路径拼接时，统一使用 `Path.resolve()` 结合前缀校验来防止目录遍历攻击，确保访问路径严格限制在配置的根目录内。