- 所有模型类继承自 BaseModel 或 TenantModel，并遵循 PascalCase 类名自动转换为 snake_case 表名的约定。
- 服务层方法通过 _before_ 和 _after_ 钩子（如 _before_create）提供扩展点，支持子类在不重写主逻辑的情况下插入校验或事件处理。
- 控制器类采用单例模式（__new__）并结合懒加载的路由器（get_router），通过装饰器自动注册权限资源与操作。
- 统一使用 success/error 等响应辅助函数返回标准化的 JSON 结构，确保 code/message/data 字段的一致性。