- 迁移脚本必须同时实现 `upgrade()` 和 `downgrade()` 函数，确保 schema 变更可逆。
- 涉及数据清洗或重命名的迁移需使用 `helpers.py` 中的幂等函数，防止因代码先行部署导致的 UniqueViolation。
- 自动生成的迁移文件需人工清理噪音（如无关的注释变更或默认索引），并验证在多租户环境下的兼容性。
- 历史废弃的迁移脚本应保留为 no-op (pass) 以维持 revision 链的完整性，严禁物理删除已合并的脚本。