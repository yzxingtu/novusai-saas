- 所有协议执行方法均通过 `_prepare_protocol_execution_context` 预计算运行时上下文，确保日志记录与错误处理的一致性。
- 在流式响应处理中，强制使用 `try...finally` 块配合 `aclose_stream` 确保上游连接资源被显式释放。
- 对上游返回的错误对象进行深度探测（检查 `status_code`, `code`, `message` 等字段），并统一转换为内部定义的 `AIGatewayError` 子类。