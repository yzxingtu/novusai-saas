- **契约隔离**：通过 `BaseAdapter` 抽象基类定义统一的聊天、流式响应及嵌入接口，强制所有子模块（如 `openai_compatible`）遵循标准交互协议。
- **动态路由**：利用 `AdapterRegistry` 维护供应商类型到适配器类的映射，支持在运行时根据配置动态创建特定供应商的适配器实例。
- **启动装配**：在应用初始化阶段通过 `register_core_adapters` 自动注册内建适配器，确保上层业务逻辑无需感知具体供应商的实现细节。