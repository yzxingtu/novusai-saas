- 所有网关执行函数（如 execute_chat）均采用依赖注入模式，接收 gateway 实例及各类服务组件（如 token_counter, cost_calculator）作为参数。
- 计费与日志记录逻辑通过 GatewayCallLogBridge 统一封装，并在执行函数的 finally 或成功路径中异步触发，确保不阻塞主响应流程。
- 故障转移（Failover）逻辑遵循“记录失败->获取备选模型->重新执行”的模式，并在 fallback 失败时抛出原始异常以保持错误上下文。