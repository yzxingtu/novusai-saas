- 采用 `ExecutionDispatcher` 作为唯一入口，根据请求模式路由至 `ConversationEngine`、`TaskEngine` 或 `BatchEngine`。
- 基于 `BaseEngine` 抽象基类统一基础设施，通过 Mixin 组合实现提示词构建、工具循环及事件发布。
- 在分发层强制执行全链路生命周期：包括预检锁获取、配额预扣、钩子触发、执行后状态回滚及并发释放。