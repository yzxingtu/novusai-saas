- 采用 Runtime/Policy/Callbacks 分离模式：运行时负责流程控制，Policy 封装决策逻辑，Callbacks 注入外部依赖，以实现高可测试性。
- 使用 `ToolFollowupDecision` 和 `ToolRoundExecutionOutcome` 等数据类封装复杂的分支返回结果（如提前退出、继续循环或中断），避免多重返回值混乱。
- 在工具调用循环中实施严格的预算守卫（BudgetGuard），在模型调用前、工具执行后及结果聚合时多次校验资源限制。