- 采用 Mixin 模式拆分基类职责，如 `BaseEngine` 继承自 `BaseEventSupportMixin`、`BaseToolLoopSupport` 等多个支持模块。
- 在流式处理器中通过静态方法委托实现逻辑解耦，将具体业务逻辑抽取到独立的 `_support` 或 `_helpers` 模块中。
- 使用 `ExecutionStateMachine` 集中管理执行上下文，包括意图计划（Intent Plan）、预算快照及提供者故障状态。
- 通过 `TurnIOAdapter` 协议抽象传输层差异，使 `TurnExecutor` 的核心编排逻辑能够同时服务于流式与非流式场景。