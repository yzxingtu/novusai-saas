- 所有恢复相关的决策逻辑均通过 `RecoveryDecision` 数据类进行标准化封装，包含动作类型、目标意图ID及原因。
- 意图状态的更新采用不可变克隆模式，在 `update_intent_statuses` 中基于原始意图创建副本进行修改，避免副作用。
- 部分输出构建时，优先使用 `RecoveryResultNormalizer` 对工具结果进行标准化截断和格式化，确保用户体验一致性。
- 系统内部的重试提示词均通过 `render_prompt_contract` 渲染，并标记为 `internal_only=True` 以防止泄露给用户。