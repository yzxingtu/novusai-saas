- 所有技能解析结果统一封装为 `SkillResolveResult` 数据类，包含 `tools`、`capability_descriptors` 及 `warnings` 字段，确保调用方接口一致性。
- 工具定义（ToolDefinition）必须携带来源追溯信息，包括 `source_skill_id`、`source_skill_name` 和 `source_plugin`，以支持权限审计与归因。
- 解析过程中遇到的非致命错误（如单个技能加载失败）不中断整体流程，而是记录在 `result.warnings` 或 `resolution_issues` 中并跳过该技能。