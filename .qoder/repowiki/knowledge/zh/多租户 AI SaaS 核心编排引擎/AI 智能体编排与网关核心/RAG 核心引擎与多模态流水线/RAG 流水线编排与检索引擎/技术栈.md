- **pgvector**：利用 PostgreSQL 的 `pgvector` 扩展进行余弦距离向量检索（见 `retriever_vector.py`）。
- **PyMuPDF (fitz)**：用于高性能 PDF 文本及图片提取（见 `parser.py`）。
- **Redis**：用于检索结果缓存（TTL 5分钟）及异步任务进度管理（见 `retriever_cache.py`, `processor.py`）。