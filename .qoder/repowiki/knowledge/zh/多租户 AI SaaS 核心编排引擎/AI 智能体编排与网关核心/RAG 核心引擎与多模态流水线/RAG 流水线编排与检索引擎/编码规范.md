- 所有解析器和分块器均遵循统一的输入输出契约，解析器输出 `ParsedPage` 列表，分块器输出 `ChunkData` 列表。
- 检索组件采用策略组合模式，通过 `SearchKBContext` 封装知识库权重与模型签名，支持多知识库加权融合检索。
- Embedding 生成前强制执行 `clean_for_embedding` 清洗流程，去除 URL、表情符号等噪声以提升向量质量。
- 异步任务中通过 `asyncio.new_event_loop()` 手动管理事件循环，以兼容 Celery 在 Windows 环境下的 solo 池模式。