- **向量数据库**：PostgreSQL with `pgvector` extension (使用 `<->` 余弦距离运算符)。
- **全文检索**：PostgreSQL `tsvector`/`tsquery` 结合自定义 SQL Boosting 逻辑。
- **PDF 解析**：`PyMuPDF (fitz)`，支持内嵌图片提取与 Vision 模型描述。
- **Office 解析**：`python-docx` (DOCX), `openpyxl` (XLSX), `pandas` (CSV)。
- **网页解析**：`httpx` + `beautifulsoup4`。