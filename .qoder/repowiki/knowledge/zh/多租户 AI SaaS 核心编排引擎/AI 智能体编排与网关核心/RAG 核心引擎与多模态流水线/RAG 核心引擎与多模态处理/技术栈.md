- **向量数据库**：使用 PostgreSQL + pgvector 插件进行余弦距离相似度检索 (`retriever_vector.py`)。
- **全文检索**：利用 PostgreSQL `tsvector` 与 `plainto_tsquery` 实现高性能关键词匹配 (`retriever_keyword.py`)。
- **文档解析**：集成 PyMuPDF (fitz)、python-docx、openpyxl、pandas 及 BeautifulSoup4 处理异构文档格式。