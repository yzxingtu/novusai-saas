- **异步运行时**：基于 `asyncio` 实现并发执行与超时控制 (`asyncio.wait_for`, `create_subprocess_exec`)。
- **安全沙箱**：利用 `resource.setrlimit` (RLIMIT_AS) 限制子进程内存，结合 `ast` 模块进行静态代码审计。
- **网络通信**：使用 `httpx.AsyncClient` 执行 HTTP 请求，禁用重定向以强化 SSRF 防护。