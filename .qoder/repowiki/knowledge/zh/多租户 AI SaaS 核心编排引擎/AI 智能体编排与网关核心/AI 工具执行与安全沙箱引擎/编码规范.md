- 所有执行器必须继承 `BaseToolExecutor` 并实现 `execute` 与 `validate` 异步方法。
- 工具定义统一使用 `ToolDefinition` 数据类，并通过 `to_openai_schema` 适配 LLM 调用格式。
- 敏感操作（如 HTTP 请求、SQL 执行）必须在执行前通过专用的 Validator 类进行安全校验。
- 跨模块引用通过包级 `__init__.py` 的 `_EXPORTS` 映射表进行延迟导入，禁止直接循环引用。