- 所有核心数据结构（如 `TurnRecord`, `CapabilityBundle`）均采用 `dataclass` 定义，并配套提供 `to_dict` 或 `to_state_dict` 方法以实现序列化兼容。
- 模块采用延迟导入模式，在 `__init__.py` 中通过 `_EXPORT_MAP` 和 `__getattr__` 暴露公共 API，以优化启动性能并解耦内部子模块依赖。
- 错误处理遵循“观察-决策-恢复”模式，通过 `ObservedStream` 收集流式信号，由 `ProtocolRecoveryPolicy` 根据语义判断是否触发协议回退或同步救援。