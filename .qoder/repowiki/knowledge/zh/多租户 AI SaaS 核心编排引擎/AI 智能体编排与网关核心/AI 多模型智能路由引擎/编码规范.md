- 采用“显式配置优先，Tier 降级兜底”的选模策略，在无法满足首选模型时按 PREMIUM -> STANDARD -> FAST 顺序查找可用替代项。
- 所有路由分支均集成 Provider 健康检查 (`_is_provider_healthy`)，确保不将流量分发至不健康的供应商节点。
- 使用 `getattr` 安全访问 `routing_config` 及 Agent 属性，并提供默认值以兼容不同版本的配置结构。
- 在路由失败或发生异常时，强制回退至 Agent 原始绑定的 `model_id`，保证服务可用性而非直接抛出错误。