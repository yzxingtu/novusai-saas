- 在涉及多步资源扣减（如配额或限流）的逻辑中，采用“预扣减-回滚”模式，确保在后续步骤失败时能通过 `rollback` 方法恢复一致性。
- 所有 Redis 原子操作（如检查并记录速率限制）均封装为 Lua 脚本执行，以消除竞态条件（TOCTOU）。
- 模块内部广泛采用依赖注入或显式参数传递（如传入 `db` 会话或配置对象），避免在工具类中隐式依赖全局状态。
- 提示词模板通过 `PromptContractName` 枚举进行强类型标识，并由 `render_prompt_contract` 统一加载渲染，禁止硬编码 Prompt 字符串。