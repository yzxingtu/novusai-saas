- 插件入口类统一继承 `app.plugins.base.PluginBase` 并实现生命周期钩子（on_install, on_enable 等）。
- 后端 API 处理器统一采用 `(request, ctx)` 双参数签名，通过 `ctx` 获取数据库会话与上下文信息。
- 数据模型统一使用 SQLAlchemy 2.0 风格（`Mapped`, `mapped_column`），表名以 `px_` 前缀标识插件私有表。
- 多租户隔离通过在查询中强制注入 `tenant_id` 过滤条件实现，管理员视角映射为平台 ID (0)。