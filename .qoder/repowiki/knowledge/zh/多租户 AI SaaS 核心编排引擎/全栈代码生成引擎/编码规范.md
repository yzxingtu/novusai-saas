- 配置解析阶段自动将 `searchable: true` 等简写字段展开为包含 `filterable`、`search.enabled` 等细节的完整结构。
- 所有生成的后端模块（models/schemas/services/repositories）均遵循按 `module` 分目录的组织规范，并自动维护各层级 `__init__.py` 的导出。
- 文件写入采用“临时目录预写 -> 校验 -> 移动覆盖”的原子操作模式，并在覆盖前自动创建带时间戳的备份目录。
- 针对 `__init__.py` 和路由文件的修改采用幂等追加策略，通过正则匹配或标记位检测避免重复注入 import 语句或路由注册代码。