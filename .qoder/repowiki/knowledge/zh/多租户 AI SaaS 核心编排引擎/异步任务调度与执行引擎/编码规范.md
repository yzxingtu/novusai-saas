- 所有任务函数必须使用 `@register_task` 装饰器进行注册，并显式指定 `queue` 和 `description`。
- 任务内部涉及数据库操作时，必须使用 `sync_session_factory()` 获取同步会话，并在 `finally` 块中确保关闭。
- 任务返回值统一为 `dict` 类型，用于记录执行状态、统计信息或错误详情，便于后续审计与监控。
- 采用 fail-closed 策略，在 `TenantTask` 中若租户资格校验失败或参数缺失，直接抛出 `Ignore` 异常终止任务而非无限重试。