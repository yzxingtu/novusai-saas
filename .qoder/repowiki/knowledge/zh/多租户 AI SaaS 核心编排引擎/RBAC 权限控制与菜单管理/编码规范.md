- 采用“单一声明原则”，权限装饰器同时负责元数据注册与运行时自动校验，避免在代码中重复定义权限逻辑。
- 服务层采用“门面模式 + 领域拆分”，将复杂的权限逻辑拆解为 Aggregation、Presentation、Check 等独立领域类，由主服务类统一协调。
- 权限同步采用“软删除”策略，当代码中移除某个权限定义时，数据库中对应的记录仅标记为禁用（is_enabled=False）而非物理删除。
- 菜单与权限元数据深度集成，通过 `MenuConfig` 和 `MenuAIConfig` 在权限定义中直接嵌入前端路由、图标及 AI 语义信息。