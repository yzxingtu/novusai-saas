- CLI 入口文件统一采用 try-catch 包裹主逻辑，并在捕获错误时调用 process.exit(1) 确保异常退出码。
- 所有子命令定义遵循 defineXxxCommand 命名规范，接收 CAC 实例作为参数进行命令注册，实现逻辑解耦。
- 脚本文件优先使用 .mjs 或 .ts 扩展名，并在 package.json 中显式声明 type: module，强制启用 ES Module 标准。