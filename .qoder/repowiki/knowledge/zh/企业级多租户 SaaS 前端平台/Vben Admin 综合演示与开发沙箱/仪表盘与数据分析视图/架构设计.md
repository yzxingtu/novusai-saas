- 模块分为`analytics`（数据分析）和`workspace`（工作区）两个子目录。
- `analytics/index.vue`作为分析页入口，组合了趋势、访问量、来源及销售等多个原子化图表组件。
- `workspace/index.vue`整合了用户信息、项目进度、待办事项及快捷导航，并复用了部分分析图表。
- 所有图表组件均基于`@vben/plugins/echarts`封装的`EchartsUI`与`useEcharts`实现，采用声明式配置渲染。