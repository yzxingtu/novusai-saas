- **统一启动引导**：通过 `bootstrap.ts` 集中初始化国际化、Pinia 状态管理、路由守卫及全局指令（如权限、加载），实现各业务模块的无缝挂载。
- **标准化 API 交互**：在 `api/request.ts` 封装统一的请求客户端，内置 Token 自动刷新、BigInt 精度处理及全局错误拦截，为所有子模块提供一致的数据通信契约。
- **模块化路由编排**：采用动态路由注册机制，将仪表盘、系统管理、功能演示等子模块的路由配置按功能域拆分并统一聚合，配合权限指令实现细粒度的访问控制。