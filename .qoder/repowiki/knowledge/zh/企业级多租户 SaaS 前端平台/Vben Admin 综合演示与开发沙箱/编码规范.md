- API 模块采用领域驱动分包（core, system, examples），并通过 `request.ts` 统一导出请求实例。
- 视图组件遵循 `views/[domain]/[feature]/index.vue` 的目录结构，复杂页面配合 `modules/` 子目录拆分逻辑。
- 多语言资源按功能域拆分为独立的 JSON 文件（如 `system.json`, `examples.json`），并在 `locales` 中统一加载。