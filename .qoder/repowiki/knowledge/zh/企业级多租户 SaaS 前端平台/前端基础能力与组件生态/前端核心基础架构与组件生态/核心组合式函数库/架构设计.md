- 采用扁平化导出结构，通过 `src/index.ts` 统一暴露所有 composables。
- 依赖 `@vben-core/shared` 获取常量与工具函数，依赖 `@vueuse/core` 实现底层响应式逻辑。
- 包含针对布局（Header/Footer/Content）的 CSS 变量动态管理逻辑，以及基于 BEM 规范的命名空间生成器。
- 使用 `unbuild` 进行构建，支持 ESM 格式输出。