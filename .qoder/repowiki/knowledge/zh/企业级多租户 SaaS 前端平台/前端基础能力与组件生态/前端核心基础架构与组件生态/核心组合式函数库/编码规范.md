- 组合式函数命名统一采用 `use-` 前缀的 kebab-case 文件名（如 `use-is-mobile.ts`）。
- 布局相关的样式操作优先通过修改 CSS 变量（CSS Variables）实现，而非直接操作 DOM 样式。
- 跨组件共享的状态型 composable（如 `useSimpleLocale`）使用 `createSharedComposable` 包装以确保单例性。