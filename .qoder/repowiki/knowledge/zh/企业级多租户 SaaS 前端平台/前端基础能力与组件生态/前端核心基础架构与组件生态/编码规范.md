- UI 功能模块采用 Monorepo 分包模式，每个子包（如 `form-ui`）均独立配置 `build.config.ts` 与 `tsconfig.json` 以实现按需编译与类型隔离。
- 复杂交互组件（如 Modal, Drawer, Form）普遍采用“逻辑 API (`use-xxx`/`xxx-api`) + 视图组件 (`.vue`)”的分离模式，支持编程式调用与声明式渲染。