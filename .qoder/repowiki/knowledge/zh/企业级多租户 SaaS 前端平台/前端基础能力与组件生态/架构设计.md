- 采用双层架构：`@core` 作为无业务逻辑的底层基础（Base/Shared），`packages` 顶层模块基于 `@core` 封装具体业务能力。
- 依赖隔离：`@core/base` 禁止引入 workspace 依赖，确保核心原子能力的纯净性与可移植性。
- 职责划分：`effects` 模块专门处理副作用（如 Pinia 状态、路由导航、API 请求），实现业务逻辑与基础 UI 的解耦。
- 统一导出：各子模块通过 `package.json` 的 `exports` 字段定义标准化入口，支持按需加载与类型声明暴露。