- 核心入口 `src/index.ts` 聚合导出本地 `UserInfo` 及 `@vben-core/typings` 的基础类型。
- `global.d.ts` 负责扩展 `vue-router` 的 `RouteMeta` 接口，并定义全局环境变量与 `Window` 对象结构。
- `shims-modules.d.ts` 集中处理无原生类型支持的第三方库（如 `secure-ls`, `json-bigint`）的模块声明。
- 依赖方向明确指向 `@vben-core/typings`，作为上层业务类型的底层基础。