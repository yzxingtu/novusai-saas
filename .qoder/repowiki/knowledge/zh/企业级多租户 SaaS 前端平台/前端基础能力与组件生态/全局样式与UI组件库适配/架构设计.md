- **入口设计**：`src/index.ts` 作为主入口，仅负责引入核心设计系统 `@vben-core/design`，确保基础样式能力的继承。
- **模块化导出**：通过 `package.json` 的 `exports` 字段定义多入口策略，将不同UI库（antd, ele, naive）及全局样式（global）隔离为独立子路径，支持按需加载。
- **适配层实现**：在 `src/antd` 等目录下通过 CSS/SCSS 文件处理特定组件库的深度样式定制（如表单验证错误状态 `.form-valid-error`、图标间距等），解决第三方库与全局设计规范的冲突。