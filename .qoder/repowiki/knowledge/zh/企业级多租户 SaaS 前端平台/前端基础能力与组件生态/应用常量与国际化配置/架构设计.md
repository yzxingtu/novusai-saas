- 入口文件 `src/index.ts` 采用聚合导出模式，既暴露本地 `core.ts` 定义的常量，也透传 `@vben-core/shared/constants` 的内容。
- `src/core.ts` 负责维护业务强相关的常量，包括路由路径 `LOGIN_PATH` 和国际化语言列表 `SUPPORT_LANGUAGES`。
- 模块通过 `package.json` 声明为 ESM 格式，并依赖 `@vben-core/shared` 工作区包以复用基础常量。