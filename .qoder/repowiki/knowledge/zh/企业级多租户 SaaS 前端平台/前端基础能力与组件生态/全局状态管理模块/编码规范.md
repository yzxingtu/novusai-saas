- 所有 Store 文件末尾均包含 `import.meta.hot.accept(acceptHMRUpdate(...))` 代码块以支持开发环境热更新。
- Store 定义统一采用 `defineStore` 函数，并遵循 `core-{module-name}` 的命名规范（如 `core-access`, `core-user`）。
- 敏感或需跨会话保持的状态（如 Token、锁屏状态）通过 `persist` 配置项明确指定 `pick` 字段进行白名单持久化。