- 作为 @vben-core/preferences 的轻量封装层，通过 defineOverridesPreferences 函数暴露配置覆写能力。
- 入口文件 src/index.ts 同时导出核心库的全部类型与工具（export * from '@vben-core/preferences'），保持 API 兼容性。
- 依赖 @vben-core/typings 提供的 DeepPartial 类型，确保覆写配置的局部更新语义。
- 构建配置继承自 @vben/tsconfig/web.json，采用 ESM 模块规范（package.json 中 type: module）。