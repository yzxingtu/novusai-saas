- 采用模块化分层设计：`basic/` 负责核心业务布局（含 Header、Sidebar、Tabbar、Content），`authentication/` 负责登录注册等认证场景，`widgets/` 封装通用 UI 交互组件（如 Preferences、GlobalSearch）。
- 以 `BasicLayout.vue` 为入口，通过组合 `@vben-core/layout-ui` 底层组件与各子模块实现复杂布局逻辑，并监听 `preferences` 状态驱动视图更新。
- 导出结构清晰：根目录 `index.ts` 统一暴露四大子系统（basic, authentication, iframe, widgets），支持按需引入。