- 采用 `useXxx` 命名规范定义组合式函数，并通过 `index.ts` 进行统一的 `export *` 导出。
- 在涉及第三方 UI 库（如 Element Plus, Ant Design）时，通过监听 `preferences.theme` 变化动态更新 CSS 变量或 Token。
- 对于需要清理副作用的逻辑（如定时器、DOM 监听），在 hook 内部通过 `onUnmounted` 或 `effectScope` 确保资源释放。