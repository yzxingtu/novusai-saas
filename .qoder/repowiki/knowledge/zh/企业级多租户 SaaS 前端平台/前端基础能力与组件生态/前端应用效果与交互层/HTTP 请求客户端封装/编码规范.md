- 所有功能模块（上传、下载、SSE、拦截器）均封装为独立类，并在 `RequestClient` 构造函数中实例化并绑定方法。
- 拦截器配置采用工厂函数模式，通过传入配置对象生成符合 `RequestInterceptorConfig` 或 `ResponseInterceptorConfig` 接口的对象。
- 请求方法（get/post/put/delete）统一委托给底层的 `request` 方法，并通过泛型支持响应数据类型定义。