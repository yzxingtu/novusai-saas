- API 响应数据转换：在 API 层或 Composable 中对后端返回的 snake_case 字段进行显式映射，转换为前端通用的 camelCase 格式。
- Composable 逻辑抽取：将视图相关的复杂状态管理、数据加载及计算属性逻辑抽取为独立的 `use-xxx.ts` 文件，保持 `.vue` 文件的简洁性。
- 权限驱动的路由过滤：在 Dashboard 等核心页面中，通过 `hasAccessByCodes` 动态过滤导航卡片与操作入口，确保界面元素与用户权限保持一致。