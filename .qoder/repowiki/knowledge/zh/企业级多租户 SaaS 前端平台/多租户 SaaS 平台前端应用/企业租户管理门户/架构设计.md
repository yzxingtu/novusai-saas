- **路由编排**：通过 `router/routes/tenant` 统一定义认证（Auth）与主布局（BasicLayout）路由，将仪表盘、AI智能体详情页及个人中心等跨模块页面静态注册，其余业务菜单依赖后端动态下发。
- **API聚合**：`api/tenant/index.ts` 作为统一出口，聚合了来自 AI、系统管理及核心业务的数十个 API 模块，为视图层提供标准化的数据访问接口。
- **视图分层**：在 `views/tenant` 下按业务域（ai, system, core）物理隔离子模块，同时共享顶层的布局容器与全局状态上下文，确保租户环境下的体验一致性。