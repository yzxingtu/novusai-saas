- API 响应数据转换：在 API 文件中定义 `transformXxx` 函数，将后端的 snake_case 字段映射为前端的 camelCase 对象。
- 权限控制指令：在操作按钮上使用 `v-access:code="['resource:action']"` 进行权限校验。
- 表单与表格配置抽离：在 `data.ts` 中定义 `useXxxColumns` 和 `useXxxFormSchema`，保持视图组件简洁。
- 状态反馈机制：使用 `message.success/error` 配合 `try-catch` 处理异步操作结果，错误通常由请求拦截器统一处理。