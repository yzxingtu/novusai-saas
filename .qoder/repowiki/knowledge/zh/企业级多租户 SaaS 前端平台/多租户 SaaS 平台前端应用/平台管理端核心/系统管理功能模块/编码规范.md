- 使用 `data.ts` 集中管理表格列配置与搜索表单 Schema，并通过 `useColumns` 和 `useGridFormSchema` 导出供页面调用。
- 采用 `useCrudPage` 适配器统一处理列表页的增删改查逻辑，通过 `api` 对象注入具体的接口方法。
- 复杂页面的状态逻辑（如代码生成器配置）通过自定义 Composables（`use-xxx.ts`）进行封装，返回响应式状态与操作方法。
- 利用 `defineOptions({ name: '...' })` 显式声明组件名称，便于调试追踪与 KeepAlive 缓存匹配。