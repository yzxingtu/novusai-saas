- 在列表页与安装预览中统一调用 `resolvePluginCompatibilityProfile` 解析并展示多租户兼容性及版本适配信息。
- 所有涉及后端状态变更的操作（如启用、卸载）均通过 `Modal.confirm` 进行二次确认，并利用 `usePluginInstallProgressStore` 跟踪异步任务进度。
- 基于 `manifest.extensions` 动态推导插件类型（如 skill, hook, api），并通过 `getTypeColor/Icon/Text` 工具函数实现 UI 标签的标准化渲染。