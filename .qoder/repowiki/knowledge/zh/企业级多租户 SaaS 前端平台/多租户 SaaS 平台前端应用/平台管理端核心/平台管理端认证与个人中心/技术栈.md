- **状态管理**：使用 Pinia 结合 `TokenStorage` 实现基于 `localStorage` 的多端 Token 隔离存储（Key 格式：`{namespace}_admin_token`）。
- **UI 框架**：基于 Ant Design Vue 构建表单与布局，集成 `@vben/common-ui` 的 `AuthenticationLogin` 组件。
- **文件处理**：通过 `smartUploadFile` 实现支持秒传（Hash 预检）与大文件分片上传的智能附件管理。