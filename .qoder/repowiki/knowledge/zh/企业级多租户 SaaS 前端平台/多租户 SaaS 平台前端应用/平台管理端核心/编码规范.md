- 详情页路由采用 `hideInMenu: true` 配合 `activePath` 属性，确保从列表页进入详情时侧边栏高亮仍停留在父级菜单。
- 调试或内部工具类页面（如代码生成器）在路由元信息中设置 `hideInProduction: true`，防止在生产环境暴露。
- 核心基础路由（如登录页）被收录于 `adminCoreRouteNames` 常量，用于在全局守卫中绕过常规的权限校验逻辑。