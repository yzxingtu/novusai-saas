- 所有配置文件均使用 `$schema` 字段指向 tsconfig 官方 Schema 以提供 IDE 智能提示。
- 基础配置强制启用严格类型检查（`strict: true`、`noImplicitAny: true`、`noUnusedLocals: true` 等）。
- Web 相关配置统一设置 `moduleResolution` 为 `bundler` 以适配现代打包工具。
- 默认禁用代码生成输出（`noEmit: true`），仅在库配置中开启声明文件生成。