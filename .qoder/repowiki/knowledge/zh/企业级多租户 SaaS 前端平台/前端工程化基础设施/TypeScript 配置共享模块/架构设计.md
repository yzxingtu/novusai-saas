- 以 `base.json` 为核心基础配置，定义严格的编译选项（如 `strict`、`noImplicitAny`）。
- 通过继承机制派生出特定场景配置：`web.json` 用于通用 Web 包，`web-app.json` 进一步扩展用于 Web 应用（引入 Vite 客户端类型），`library.json` 用于库开发（开启声明文件生成），`node.json` 用于 Node.js 环境。
- `package.json` 将这些 JSON 文件作为包内容发布，供工作区其他模块通过 `@vben/tsconfig` 引用。