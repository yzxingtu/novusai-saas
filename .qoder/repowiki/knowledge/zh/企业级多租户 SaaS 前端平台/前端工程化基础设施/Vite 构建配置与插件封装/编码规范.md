- 采用条件化插件装配模式：通过 `ConditionPlugin` 数组与 `loadConditionPlugins` 统一处理插件的启用逻辑。
- 区分应用与库的配置路径：以 `defineApplicationConfig` 与 `defineLibraryConfig` 分别管理不同构建场景的插件与选项。
- 在 `src/typing.ts` 中集中定义所有插件选项与配置类型，确保调用方与实现方的类型一致性。