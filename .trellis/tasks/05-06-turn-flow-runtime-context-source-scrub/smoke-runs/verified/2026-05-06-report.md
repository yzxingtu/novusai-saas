# 2026-05-06 Verification Report

## Scope

Conversation `2340` regression: provider failure with no selected/candidate
tools must not show fake source counts or completed-looking retrieval chrome.

## Structural

- `cd backend; python -m ruff check app\ai\engine\turn_flow_projector.py app\services\ai\conversation_turn_flow_projector.py app\cli_commands\ai_snapshot.py tests\regressions\test_bug_2026_05_06_2340_turn_flow_context_sources.py tests\ai\engine\test_turn_flow_projector.py tests\test_ai_conversation_cli.py`
  - Result: passed.
- `cd backend; python scripts/check_prompt_contracts.py`
  - Result: passed.
- `cd frontend; pnpm --dir apps/web-antd exec vue-tsc --noEmit --skipLibCheck --pretty false -p tsconfig.json`
  - Result: passed.

## Behavioral

- `cd backend; python -m pytest tests\test_ai_conversation_cli.py tests\regressions\test_bug_2026_05_06_2340_turn_flow_context_sources.py tests\services\test_conversation_turn_flow_projector.py tests\ai\engine\test_turn_flow_projector.py -q`
  - Result: `48 passed`.
- `pnpm --dir frontend/apps/web-antd exec vitest run --dom src/components/business/ai-chat-kernel/__tests__/ChatMessageKernel.test.ts src/components/business/ai-chat-kernel/__tests__/TurnTimeline.test.ts src/components/business/ai-chat-kernel/__tests__/TurnFlowState.test.ts src/components/business/ai-chat-panel/__tests__/chat-message-turn-flow-core.test.ts src/components/business/ai-chat-panel/__tests__/use-ai-chat.test.ts src/components/business/ai-chat-panel/__tests__/process-i18n.test.ts`
  - Result: `6 passed`, `80 passed`.

## CLI Smoke

- `cd backend; python -m app.cli ai conversation show 2340 --tail 12 --diagnostics-only --json`
  - Result: failed provider layer, `selected_tool_names=[]`,
    `candidate_tool_names=[]`, `tool_rounds_used=0`,
    `failure_kind=provider_unavailable`.
- `cd backend; python -m app.cli ai conversation show 2340 --tail 12 --json`
  - Result: full CLI JSON no longer contains `Retrieved 3 sources` or
    `"source_count": 3`; projected assistant turn-flow has
    `evidence=[]`, `source_chip_ids=[]`, retrieval `status=skipped`, and
    retrieval `source_count=0`.

## Browser Regression

- `$env:CI='1'; $env:TENANT_E2E_PORT='5678'; pnpm --dir frontend/apps/web-antd exec playwright test ai-chat-turn-flow-regression.spec.ts --config=playwright.config.ts`
  - Result: Chromium browser regression passed. The fixture mounts the real
    `ChatMessageKernel`, real `TurnTimeline`, and real `buildTurnFlowState` in
    a browser, expands the timeline, and asserts no fake source chrome is
    rendered. This is browser behavioral coverage, not a provider real-dialogue
    smoke claim.

## Provider Smoke

- Real provider replay was not claimed for this task. The original incident is
  an upstream `provider_unavailable` persisted conversation; verification uses
  the exact stored conversation `2340`, CLI read-model normalization, behavioral
  regressions, and browser rendering of the polluted payload.

## Cross-Audit

- Backend projection/CLI audit: no blocking issue; the obsolete
  `context_sources -> evidence` helper was deleted after audit feedback.
- CLI conversation `2340` audit: all nested user-facing `turn_flow` payloads
  have `evidence=[]`, `source_chip_ids=[]`, retrieval `status=skipped`, and
  `source_count=0`.
- Git boundary audit: unrelated rich-text / AI writing / migration files were
  left unstaged.
- Online-search removal audit: no `web_search`, `fetch_url`, provider-hosted,
  or native-search live path was restored.
- Frontend timeline audit: fixed the raw `msg.turnFlow` retrieval-source bypass
  by deriving expanded timeline retrieval rows from sanitized `TurnFlowState`
  evidence, then added a regression for expanded timeline fake-source
  suppression.

## Residual Risk

This verification does not replay a live provider call because the original
provider failure is upstream/network-dependent. It verifies the exact persisted
conversation `2340`, the same failure shape as behavioral fixtures, and real
browser rendering of the polluted turn-flow payload.
