# NovusAI SaaS Backend

依赖以 **`pyproject.toml`** 声明、**`uv.lock`** 锁定版本（须提交到仓库）。

## 安装

```bash
uv sync --extra dev
```

未安装 [uv](https://docs.astral.sh/uv/) 时：

```bash
pip install -e ".[dev]"
```

更多环境变量、迁移与启动命令见仓库根目录 **`README.md`**。
