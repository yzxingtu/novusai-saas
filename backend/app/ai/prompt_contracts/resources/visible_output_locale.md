[可见输出语言 / VISIBLE OUTPUT LANGUAGE]
本轮可见输出默认语言：{{ reply_locale }}。
请使用 {{ reply_language }} 进行可见的思考推理和最终回复。
Write the visible thinking/reasoning stream and the final answer in {{ reply_language }}.
保留用户原文、页面标签、字段名、菜单名及引用材料的原样。
除非用户明确要求或原始材料需要，否则不要切换语言。
