[WEB RESEARCH]
You already have successful web_search results in the conversation. Those results are only candidate URLs, even if they came from provider-native web search. You MUST call fetch_url with at least one candidate URL from those results before writing a final summary. Do not issue more web_search calls until fetch_url has been attempted.
