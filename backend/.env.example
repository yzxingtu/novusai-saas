# ============================================
# NovusAI SaaS Backend Environment Variables
# ============================================
# Copy this file to .env and modify the values

# ============================================
# Application Settings
# ============================================
APP_NAME=NovusAI SaaS
APP_VERSION=0.1.0
APP_ENV=development  # development, staging, production
DEBUG=true

# Timezone (used for backend logic and database)
# Examples: Asia/Shanghai, UTC, America/New_York
TIMEZONE=Asia/Shanghai

# API Settings
API_V1_PREFIX=/api/v1

# CORS Settings (comma-separated)
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# ============================================
# Security Settings
# ============================================
# IMPORTANT: Generate a secure secret key for production
# You can use: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production

# JWT Token Settings
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7
ALGORITHM=HS256

# ============================================
# Database Settings (PostgreSQL)
# ============================================
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=novusai_saas

# Connection Pool
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30

# ============================================
# Redis Settings
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================
# Celery Settings
# ============================================
# Leave empty to use Redis URL
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

# ============================================
# AI Data Intelligence (Text-to-SQL)
# ============================================
# Readonly database URL for AI queries
# Create the user first: see scripts/create_ai_readonly_user.sql
# AI_READONLY_DB_URL=postgresql://ai_readonly:your_password@localhost:5432/novusai_saas

# ============================================
# Logging Settings
# ============================================
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_DIR=logs

# ============================================
# Pagination Settings
# ============================================
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
