/**
 * 企业网盘插件样式 — 全局注入的最小 CSS
 * 所有 nd-* 布局/组件类已迁移至内联样式或 Ant Design Vue 组件
 * 此文件仅保留无法内联的动画 keyframes
 */
export const NETDISK_STYLES = `;
/* nd-spin 动画 - 供 SidebarTree 加载图标使用 */
@keyframes nd-spin { to { transform: rotate(360deg); } }
`;

