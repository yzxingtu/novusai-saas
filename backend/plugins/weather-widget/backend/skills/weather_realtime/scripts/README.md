# Implementation Notes

Runtime resolution is provided by `backend/skills/weather_resolver.py`.

Execution is provided by `backend/executors/weather_widget_executor.py`.

This directory is reserved for future helper scripts that belong to the
`weather_realtime` skill package structure.
