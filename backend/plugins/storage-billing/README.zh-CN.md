# 对象存储对账计费插件

这是对象存储流量对账计费的独立插件目录。

当前骨架已包含：

- `manual_entitlement` 企业侧授权策略，避免插件启用时自动授权所有套餐。
- “只按官方账单计费”的范围声明，明确仅支持七牛云、阿里云 OSS、腾讯云 COS，本地存储不计费。
- 每天 `03:00` 的对账任务占位注册，以及管理端/企业端占位 API。

后续实现将继续补齐：

1. 三家云厂商官方账单适配器。
2. `px_storage_billing_` 前缀的插件自有台账与结算表。
3. 每日结算单生成、导出与租户账单视图。
4. 企业端前端页面与报表交互。
