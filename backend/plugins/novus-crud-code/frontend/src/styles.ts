/**
 * DataForge Studio Plugin - Minimal CSS for SortableJS drag feedback
 */
export const NCC_STYLES = `
.ncc-fb-ghost { opacity: 0.4; border: 2px dashed hsl(var(--primary)) !important; }
.ncc-fb-chosen { box-shadow: 0 4px 12px hsl(var(--primary) / 0.25); }
`;
