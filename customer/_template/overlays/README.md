# Deployment And Configuration Overlays

Put customer-specific deployment and configuration overlays here.

Examples:

- Compose override examples
- Helm values
- ingress and reverse-proxy snippets
- environment examples without secrets
- platform-specific runtime settings

Do not store real secrets in this directory.
